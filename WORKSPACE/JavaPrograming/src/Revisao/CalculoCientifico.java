package Revisao;

public class CalculoCientifico  extends Calculo{


    public  double raizQuadrada(double a ){

        double raiz = Math.sqrt(a);
        return raiz;
    }

    public double exponenciacao(double a , double b){
        double expo = Math.pow(a,b);
        return  expo;
    } // quando trabalhar com return quando vai usar mais para frente no código



}
